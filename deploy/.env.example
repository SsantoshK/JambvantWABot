### Example environment values for `deploy/docker-compose.yml`
# Copy this file to `.env` on the VPS and update values before `docker-compose up -d`
# NOTE: These are example placeholders — DO NOT commit real credentials to git.
# Replace the `replace_with_...` values with strong generated secrets before deploying.

# Postgres
POSTGRES_USER=jambvant_user
POSTGRES_PASSWORD=replace_with_a_strong_password_here
POSTGRES_DB=jambvant_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# n8n basic auth (protect the n8n UI)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=replace_with_a_strong_password_here

# Public URL where n8n webhooks will be reachable
WEBHOOK_URL=https://jambvant.com
VUE_APP_URL_BASE_API=https://jambvant.com
